extends layout.pug

block contenido
	div(class="col-md-6 remove-float center-block biggest-top-space")
		h1 Inicia Sesión
		form(action="/sessions", method="POST")
			div(class="form-group")
				label(for="email") Email
				input(type="email", name="email", placeholder="Email", id="email", class="form-control")
			
			div(class="form-group")
				label(for="password") Password
				input(type="password", name="password", placeholder="introduzca password", id="password", class="form-control")
			
			div(class="row form-group top-space")
				div(class="col-xs-12 col-sm-6")
					input(type="submit", value="Ingresar", class="btn btn-success")
				div(class="col-xs-12 col-sm-6 text-right")
					a(href="/signup") No tengo cuenta
		